// Un camión transporta 25 cajas de repuestos de carros. Si cada caja pesa 748 Kg ¿cuántos
// Kg transporta?

let pesoCajas = 748;
let pesoCamion = 25;
let pesoTotal = pesoCajas * pesoCamion;

alert(`El peso total que transporta el camion es de: ` + pesoTotal);
