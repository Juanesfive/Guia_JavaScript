// Hacer un diagrama para convertir de grados centígrados a grados Fahrenheit

let grados = parseInt(prompt("Ingrese los grados centígrados"));

alert(`Los grados Fahrenheit son: ` + ((grados * 9/5) + 32));