// Escribir un programa que calcule el área de un rectángulo:


let largo = parseInt(prompt("Ingrese el largo del rectángulo"));
 
let ancho = parseInt(prompt("Ingrese el ancho del rectangulo"));

alert(`El área de este rectangulo teniendo en cuenta que el largo es ${largo} y el ancho es ${ancho} es: ` + (largo * ancho));